<Project Sdk="Microsoft.NET.Sdk">

  <!--
    Camada mais interna da arquitetura: blocos de dominio (Entity, ValueObject,
    Result, excecoes de negocio) reaproveitados por todos os microsservicos.

    Regra de ouro: este projeto NAO referencia nada. Sem EF Core, sem MassTransit,
    sem ASP.NET. Se um dia precisar, e sinal de que a regra de negocio esta vazando
    para a infraestrutura.

    TargetFramework, Nullable e ImplicitUsings vem do Directory.Build.props da raiz.
  -->

</Project>
